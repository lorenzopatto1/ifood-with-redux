import { RestaurantOption } from '../../components/RestaurantOption';
import { Container } from "./styles.ts";

export const Home = () => {
    return (
        <Container>
          <RestaurantOption />
        </Container>
    );
};
